# Indexer App

Indexer service for Nest Markets events, market state, and analytics data.

Suggested next step:
- Initialize the indexer runtime here (for example, Node.js + PostgreSQL + queue worker).
